{
  "name": "bruce",
  "version": "0.0.7",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "yarn build:banners && yarn build:dash",
    "build:banners": "ts-node ./prebuild.ts",
    "build:dash": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "count": "ts-node count.ts"
  },
  "dependencies": {
    "fn": "*",
    "gygax": "*",
    "vui": "*"
  },
  "devDependencies": {
    "ntl": "*",
    "tsconfig": "*"
  }
}
